# Brotli dynamic modules — remove or empty this file to disable Brotli entirely.
# Gzip (configured in nginx.conf) serves as the fallback compression.
load_module modules/ngx_http_brotli_filter_module.so;
load_module modules/ngx_http_brotli_static_module.so;
