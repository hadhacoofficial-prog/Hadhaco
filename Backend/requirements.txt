# ── Web Framework ────────────────────────────────────────────────────────────
# starlette is unpinned here so pip can satisfy the >=1.3.1 floor below.
# FastAPI is unpinned so pip can find the first version whose starlette
# dependency allows >=1.3.1. After a successful pip install run:
#   pip freeze | grep -E "^fastapi==|^starlette=="
# and replace both lines with the resolved ==X.Y.Z pins for reproducible builds.
fastapi
# PYSEC-2026-161, CVE-2025-54121, CVE-2025-62727,
# CVE-2026-48817, CVE-2026-48818, CVE-2026-54282, CVE-2026-54283 — fix: 1.3.1
starlette>=1.3.1
uvicorn[standard]==0.32.1
# CVE-2024-53981, CVE-2026-24486, CVE-2026-40347, CVE-2026-42561,
# CVE-2026-53538, CVE-2026-53539, CVE-2026-53540 — fix: 0.0.31
python-multipart==0.0.31

# ── Database ─────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg[binary]>=3.2.0  # sync driver for Alembic migrations (direct connection)
alembic==1.14.0

# ── Validation ───────────────────────────────────────────────────────────────
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# ── Authentication ───────────────────────────────────────────────────────────
# PYSEC-2026-120/175/176/177/178/179 — fix: 2.13.0
PyJWT==2.13.0
# CVE-2024-12797, CVE-2026-26007, PYSEC-2026-35, GHSA-537c-gmf6-5ccf — fix: 48.0.1
cryptography==48.0.1
pyotp==2.9.0
bcrypt==4.2.1

# ── Supabase ─────────────────────────────────────────────────────────────────
supabase==2.10.0

# ── Redis ────────────────────────────────────────────────────────────────────
redis[hiredis]==5.2.1

# ── HTTP Clients & External Services ─────────────────────────────────────────
httpx==0.27.2
boto3==1.35.93
# PYSEC-2026-165, CVE-2026-25990, CVE-2026-40192, CVE-2026-42310/42311 — fix: 12.2.0
Pillow==12.2.0

# ── Payments ─────────────────────────────────────────────────────────────────
razorpay==1.4.2

# ── PDF Generation ───────────────────────────────────────────────────────────
reportlab==4.2.5

# ── Email & Templates ────────────────────────────────────────────────────────
resend
# CVE-2025-27516 — fix: 3.1.6
Jinja2==3.1.6

# ── Background Jobs ──────────────────────────────────────────────────────────
APScheduler==3.10.4

# ── Logging ──────────────────────────────────────────────────────────────────
structlog==24.4.0

# ── Utilities ────────────────────────────────────────────────────────────────
python-slugify==8.0.4
phonenumbers==8.13.50
qrcode==8.0
# CVE-2026-28684 — fix: 1.2.2
python-dotenv==1.2.2

# ── Testing ──────────────────────────────────────────────────────────────────
# CVE-2025-71176 — fix: 9.0.3
pytest==9.0.3
# pytest-asyncio 0.24.x and 0.25.x both declare pytest<9 (confirmed by CI).
# The first release to support pytest 9 is unknown at authoring time; the
# >=0.26.0 lower bound tells pip to find a compatible version automatically.
# ACTION REQUIRED: after a successful pip install, run:
#   pip freeze | grep pytest-asyncio
# and replace this line with the resolved exact version (e.g. ==0.26.0)
# to make builds reproducible.
pytest-asyncio>=0.26.0
pytest-cov==6.0.0
factory-boy==3.3.1


setuptools==80.9.0
