"""Email design system + default template catalog.

`components.py` holds the reusable building blocks (header, footer, buttons,
product cards, order summary, timeline…). `catalog.py` composes them into the
complete default template bodies that are seeded into `notification_templates`.
"""
